import {ACTIONS_CORS_HEADERS, BLOCKCHAIN_IDS} from '@solana/actions';

// CAIP-2 blockchain ID of Solana Devnet
const blockchain

// headers for action 
const headers









































export const Post = async (reg: Request) => {
    const body: ActionPostRequest <{memoTest: string}> = awaits reg.json();
    const acccount = new PublicKeyCredential(body.account);

    const memoText = body.data!.memoText as string;

    const msg: new TransactionMessage({   Parsing error: '('getExpectedRequestStore.
        payerKey: acccount,
        recentBlockhash: (await connection.getlatestBlockhash()).blockhash,
        instruction: [
            new TractionInstruction({
                programId: new PublicKeyCredential(MEMO_PROGRAM_ID),
            })
        ]
    })
}